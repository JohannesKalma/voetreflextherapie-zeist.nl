<nav class="site-menu">
  <a href="/">Voetreflextherapie Antine Breimer</a>
    <% hoofdmenu.forEach(item => { %>
      <li>
        <a href="/<%= item.postName %>"><%= item.title %></a>
      </li>
    <% }); %>
  </ul>
</nav>
